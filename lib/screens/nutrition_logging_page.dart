import 'package:flutter/material.dart';

class NutritionLoggingPage extends StatelessWidget {
  const NutritionLoggingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Nutrition Logging Page Content'),
    );
  }
}
